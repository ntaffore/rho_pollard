\r algo_rho_negation2.gp
\r initiation_courbe.gp

/* création de la courbe elliptique */
n = prem_n_bit(43);
E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

E = courbe_ell_Fp(n);

/* prise d'un point aleatoire sur E */
P = random_point_curve(E);
pow = random(n);

Q = ellpow(E,P,pow);

/* demarage du compteur temps */
#
pow_exp = rho_p_V5(E,P,Q);

/* fin du temp */
#

